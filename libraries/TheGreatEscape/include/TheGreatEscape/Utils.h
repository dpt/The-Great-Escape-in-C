#ifndef UTILS_H
#define UTILS_H

#define UNKNOWN 1

#define NELEMS(a) ((int) (sizeof(a) / sizeof(a[0])))

#define MIN(a,b) ((a) < (b) ? (a) : (b))
#define MAX(a,b) ((a) > (b) ? (a) : (b))

#endif /* UTILS_H */

